# OpenRouter API key used by `just bootstrap openrouter`, `just configure-inference-openrouter`,
# and `just deploy-canister` (optional, but required for OpenRouter inference to work).
OPENROUTER_API_KEY=replace-with-your-openrouter-api-key

# Optional OpenRouter API base URL override.
# Leave empty to use canister default: https://openrouter.ai/api/v1
OPENROUTER_BASE_URL=

# Optional default OpenRouter model for `just bootstrap` and `just configure-inference-openrouter`.
OPENROUTER_MODEL=openai/gpt-4o-mini

# Optional default IcLlm model for `just bootstrap`, `just ollama-start`, and `just configure-inference-icllm`.
IC_LLM_MODEL=llama3.1:8b

# Optional Base mainnet RPC used when running `base-fork` mode.
BASE_MAINNET_RPC_URL=https://mainnet.base.org
